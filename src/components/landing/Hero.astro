---
import Button from "@/components/ui/Button.astro";
---

<section
    class="relative min-h-screen flex items-center justify-center overflow-hidden bg-[#0F0F0E]"
>
    <!-- Background Particles (CSS Implementation for performance) -->
    <div class="absolute inset-0 z-0 opacity-30">
        <div class="particle-container">
            {
                Array.from({ length: 20 }).map((_, i) => (
                    <div
                        class="particle"
                        style={`--delay: ${Math.random() * 5}s; --duration: ${10 + Math.random() * 20}s; --left: ${Math.random() * 100}%; --top: ${Math.random() * 100}%`}
                    />
                ))
            }
        </div>
    </div>

    <!-- Golden Glow Gradient -->
    <div
        class="absolute top-0 left-1/2 -translate-x-1/2 w-full h-[500px] bg-brand-light/5 blur-[120px] rounded-full pointer-events-none z-0"
    >
    </div>

    <div
        class="container relative z-10 px-6 mx-auto grid lg:grid-cols-2 gap-12 items-center"
    >
        <!-- Left: Copy -->
        <div class="text-left space-y-8">
            <div class="overflow-hidden">
                <h1
                    class="text-5xl md:text-7xl font-serif text-content-primary leading-tight animate-fade-up opacity-0"
                    style="animation-delay: 0.2s;"
                >
                    La Primera Impresión de su Firma Debe Ser <span
                        class="text-brand italic">Impecable.</span
                    >
                </h1>
            </div>

            <div class="overflow-hidden">
                <p
                    class="text-lg md:text-xl text-content-secondary font-light max-w-xl leading-relaxed animate-fade-up opacity-0"
                    style="animation-delay: 0.6s;"
                >
                    Automatice la admisión de clientes, la firma de contratos y
                    la recolección de documentos en una sola <span
                        class="text-content-primary font-normal"
                        >"Sala de Bienvenida"</span
                    > digital. Su despacho, modernizado.
                </p>
            </div>

            <div
                class="flex flex-col sm:flex-row gap-4 animate-fade-up opacity-0"
                style="animation-delay: 1s;"
            >
                <Button
                    href="/auth/signup?plan=boutique"
                    variant="primary"
                    size="lg"
                    class="group relative overflow-hidden"
                >
                    <span class="relative z-10">SOLICITAR ACCESO</span>
                    <div
                        class="absolute inset-0 bg-white/20 translate-y-full group-hover:translate-y-0 transition-transform duration-500 ease-out"
                    >
                    </div>
                </Button>
                <Button href="#features" variant="outline" size="lg">
                    VER DEMO
                </Button>
            </div>
        </div>

        <!-- Right: Visual Mockup -->
        <div
            class="relative hidden lg:block animate-fade-in opacity-0"
            style="animation-delay: 1.2s;"
        >
            <!-- Abstract 3D Card Effect -->
            <div class="relative w-full aspect-[4/5] perspective-1000">
                <div
                    class="absolute inset-0 bg-gradient-to-br from-surface-card to-surface-base border border-border/50 rounded-sm shadow-2xl transform rotate-y-12 rotate-x-6 hover:rotate-y-0 hover:rotate-x-0 transition-transform duration-1000 ease-out p-6 flex flex-col gap-4 group"
                >
                    <!-- Mockup Header -->
                    <div
                        class="flex items-center justify-between border-b border-border/30 pb-4"
                    >
                        <div
                            class="w-24 h-6 bg-border/20 rounded-sm animate-pulse"
                        >
                        </div>
                        <div class="w-8 h-8 rounded-full bg-brand/20"></div>
                    </div>
                    <!-- Mockup Body -->
                    <div class="space-y-4 flex-1">
                        <div class="w-3/4 h-8 bg-content-primary/10 rounded-sm">
                        </div>
                        <div
                            class="w-full h-32 bg-surface-highlight/30 rounded-sm border border-dashed border-border/30 flex items-center justify-center"
                        >
                            <span
                                class="text-xs text-content-muted uppercase tracking-widest"
                                >Firma Digital</span
                            >
                        </div>
                        <div class="space-y-2">
                            <div class="w-full h-2 bg-border/20 rounded-full">
                            </div>
                            <div class="w-2/3 h-2 bg-border/20 rounded-full">
                            </div>
                        </div>
                    </div>
                    <!-- Mockup Button -->
                    <div
                        class="w-full h-10 bg-brand text-surface-base flex items-center justify-center text-sm font-medium tracking-wide rounded-sm shadow-lg shadow-brand/20"
                    >
                        CONFIRMAR
                    </div>

                    <!-- Floating Elements -->
                    <div
                        class="absolute -right-12 top-1/4 bg-surface-card border border-border p-4 rounded-sm shadow-xl transform translate-z-10 group-hover:translate-x-2 transition-transform duration-700"
                    >
                        <div class="flex items-center gap-3">
                            <div class="w-2 h-2 rounded-full bg-status-success">
                            </div>
                            <span class="text-xs text-content-primary"
                                >Contrato Firmado</span
                            >
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Golden Line Connector -->
    <div
        class="absolute bottom-0 left-1/2 -translate-x-1/2 w-[1px] h-24 bg-gradient-to-b from-transparent via-brand to-brand opacity-50"
    >
    </div>
</section>

<style>
    .perspective-1000 {
        perspective: 1000px;
    }

    .rotate-y-12 {
        transform: rotateY(-12deg) rotateX(5deg);
    }

    /* Particle Animation */
    .particle {
        position: absolute;
        left: var(--left);
        top: var(--top);
        width: 2px;
        height: 2px;
        background-color: #c8aa6e;
        border-radius: 50%;
        animation: float var(--duration) ease-in-out infinite;
        animation-delay: var(--delay);
        opacity: 0.4;
    }

    @keyframes float {
        0%,
        100% {
            transform: translate(0, 0);
            opacity: 0.2;
        }
        50% {
            transform: translate(10px, -20px);
            opacity: 0.6;
        }
    }

    /* Fade Up Animation */
    @keyframes fadeUp {
        from {
            opacity: 0;
            transform: translateY(20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .animate-fade-up {
        animation: fadeUp 1s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
    }

    .animate-fade-in {
        animation: fadeIn 1.5s ease-out forwards;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }
</style>
